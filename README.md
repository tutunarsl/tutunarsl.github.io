# tutunarsl.github.io

Personal website source for GitHub Pages.

## Run locally

This is a static site, so there is no build step. You can preview it locally using Python’s built-in HTTP server.

### 1) Go to the project directory

```bash
cd /home/tutuna/personal_page_ws/src/tutunarsl.github.io
```


### 2) Start a local server 
```bash
python3 -m http.server 8080
```

### 3) Open the site in your browser

```bash
xdg-open http://localhost:8080
```
